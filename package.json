{
    "name": "bigboyturbo",
    "version": "0.0.0",
    "scripts": {
        "deploy": "echo www.bigboyturbo.com > CNAME && cp index.html 404.html && git init && git remote add origin https://github.com/pushpatech/bigboyturbo.git && git add . && git commit -m 'Deploy' && git branch -M main && git push -u origin main --force"
    }
}